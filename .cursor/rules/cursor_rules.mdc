ALWAYS refer to `docs/README.md`.

ALWAYS follow `docs/rules/` and the docs referenced from `docs/README.md`.

NEVER store substantive guidance in this wrapper. Keep all important documentation in `docs/`.
